#include <stdio.h>
/**
 * main- print all single digit numbers starting from 0
 *
 * Return: always 0 (success)
 *
 */
int main(void)
{
int i;

for (i = 0; i <= 9; i++)
{
	printf("%d", i);
}
return (0);
}
