#include "main.h"
/**
 * _abs - Returns the absolute value of any integer
 *
 * @a : integer number
 * Return: Always 0
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-1 * a);
	}
}

